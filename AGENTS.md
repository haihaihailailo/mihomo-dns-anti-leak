# 项目工作规则

- 真实项目为本 Git checkout；先读本文件、README 的开发/验证章节和 `.github/artifact-policy.json`。旧日期任务目录、安装客户端和服务器不是源码权威。
- 唯一离线验收入口：`python .github/scripts/validate_health_checks.py`。新增生命周期回归也必须由此调用；不得绕过超时、内存和合成夹具边界。可选公网/内核验收需单独显式启用，不代表手机或原生苹果客户端通过。
- 十个公开入口由共同源码生成；使用 `npm run build:profiles`，不手改生成结果。路由器两份 YAML 由已调优的 Mihomo 两地配置投影，移除进程规则与设备字段；订阅、节点、客户端和系统设置不属于本项目生成物。
- 新规则快照、端点报告、隔离内核输出只进入 `.generated/runs/`，通过 `artifact_lifecycle.cjs` 登记。不得另建日期目录重复存放，也不得复制源码、node_modules、内核包到每次快照；源码依赖锁文件和 Git，内核使用外部已验证路径。
- 每条生成产品线保留最新已验证候选、显式登记的正式恢复套件及其完整依赖、活动断点和最近一次失败现场。同一套的 ZIP、安装器、源码和证据若承担不同职责，不得按哈希以外的名称判断重复。
- 正式源码的恢复根是 Git；本仓库没有自动打包/发布程序。不得将仅通过下载/离线检查的快照冒充正式发布恢复包。发布和改动正式恢复根仍需独立授权。
- 自动回收仅针对本工具新登记、身份/哈希稳定、非链接、无占用、无保留根依赖的普通文件。保护源码、Git、业务、凭据、未知/不可读对象及旧未登记目录；不能按目录名、年龄接管存量。两项历史长路径缓存继续保留。
- 普通生成日志保留最多 7 天且合计不超过 100 MiB；恢复清单和删除审计独立保留并计入总预算。开始生成前预留容量，超预算先对已登记且无引用对象整理；核验、占用查询、原子登记或清理失败必须停止。不得因失败继续开新目录、自动删锁、修改 ACL/属性、强杀或递归删除父目录。
- 异常中断留下锁/活动清单时停止并人工复核；不能以 PID 不存在或锁很旧为由自动接管。容量设置可在 policy 中经审查调整，不允许临时环境变量关闭保护。
- 本轮禁止额外批量清理存量、安装、部署、服务器/打印/业务操作和推送。保留所有用户已有改动。
